# names = ['Magda', 'Jose', 'Anne']
# lengths = []

# ## loop equal "lengths = list(map(len, names))"
# for name in names:
#     lengths.append(len(name))

# lengths = list(map(len, names))
# print (lengths)

